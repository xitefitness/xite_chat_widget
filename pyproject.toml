[project]
name = "xite_chat_widget"
authors = [{ name = "Xite Group" }]
description = "Floating Raven team chat button on every ERPNext Desk page"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<17.0.0"
